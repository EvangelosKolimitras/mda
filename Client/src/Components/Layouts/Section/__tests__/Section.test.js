import { shallow } from 'enzyme';
import React   from 'react';
import Section from '../Section';


describe("the Section component", () => {

    it("should render correctly Section component", () => {
        const wrapper = shallow(<Section />)
    });

});
