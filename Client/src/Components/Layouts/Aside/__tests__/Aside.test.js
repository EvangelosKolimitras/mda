import { shallow } from 'enzyme';
import React from 'react';
import Aside from '../Aside';


describe("the Aside component", () => {

    it("should render correctly Aside component", () => {
        const wrapper = shallow(<Aside />)
    });

});
