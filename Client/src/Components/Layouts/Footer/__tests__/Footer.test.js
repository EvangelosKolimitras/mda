import { shallow } from 'enzyme';
import React  from 'react';
import Footer from '../Footer';


describe("the Footer component", () => {

    it("should render correctly Footer component", () => {
        const wrapper = shallow(<Footer />)
    });

});
